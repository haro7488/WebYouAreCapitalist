# AI 경쟁사 시스템 설계

> Phase 2 핵심 기능. 매 런마다 AI 경쟁사와 함께 경쟁하는 투자 로그라이트.

---

## 절대 원칙

### 1. 동일 엔티티 원칙
플레이어와 경쟁사는 본질적으로 같은 `Company` 엔티티다. 게임 엔진은 누가 조종하는지 모른다. 보드게임의 오토마와 같은 구조.

### 2. 화폐 보존 법칙
시스템 내 총 화폐량은 변하지 않는다. 돈은 생성되지도 소멸되지도 않으며, 참여자 간에 이동할 뿐이다.

```
총 화폐 = Σ(company.cash) + Σ(asset market value) + marketPool = 상수
```

---

## Company 엔티티

```typescript
interface Company {
  id: string;
  name: string;           // "한강투자", "청담캐피탈" 등
  cash: number;
  assets: OwnedAsset[];
  influence: number;
  ap: number;
  maxAp: number;
  netWorth: number;        // cash + Σ(asset value)
  dominatedSectors: SectorId[];
}

// 컨트롤러는 엔진 바깥
interface Controller {
  type: 'human' | 'ai';
  strategy?: CompetitorStrategy;  // AI일 때만
}

// 전략 패턴
interface CompetitorStrategy {
  id: string;
  name: string;
  decide(state: GameState, company: Company): Action[];
}
```

- 엔진은 `Company[]`와 `Action[]`만 처리
- UI 레이어에서만 human/ai 구분

---

## 경쟁사 설정

| 항목 | 값 | 비고 |
|------|-----|------|
| 기본 수 | 3 | 상수로 조절 가능, 테스트 후 확정 |
| 이름 | 한국형 기업명 | 이후 로컬라이징 고려 |
| 시작 자금 | 플레이어와 동일 | 동일 엔티티 원칙 |
| AP | 플레이어와 동일 | 동일 엔티티 원칙 |

### 이름 풀 (예시)

시드 RNG로 매 런 랜덤 배정:

```
한강투자, 청담캐피탈, 밀레니엄파트너스, 남산홀딩스,
종로에셋, 강남벤처스, 을지파이낸스, 세종인베스트,
광화문자산운용, 판교테크캐피탈
```

---

## 전략 타입 (4개)

Strategy Pattern으로 교체 가능한 구조.

| 타입 | 행동 패턴 | 리스크 |
|------|----------|--------|
| 🏦 **보수형** | Tier 1~2 분산, 불황 시 현금 비축, 안전 우선 | 낮음 |
| 🚀 **공격형** | Tier 2~3 집중, 호황 시 공격적 매입, 고수익 추구 | 높음 |
| 🎯 **지배형** | 한 섹터 올인, 지배력 보너스 극대화 | 중간 |
| 🎲 **기회형** | hot 섹터 추격매수, 트렌드 전환 시 매각, 시세차익 | 높음 |

---

## 경제 모델: 시장 풀 기반

### 초기 화폐 분배

```
플레이어:         $1,000
경쟁사 3개:       $3,000 ($1,000 × 3)
시장 풀:         $20,000
─────────────────────────
총계:            $24,000 (런 내내 불변)
```

### 자산 소득 = 점유율 기반 분배

기존의 `baseIncome`(허공에서 생성) 대신, 시장 풀에서 섹터로 유입되는 돈을 점유율로 나눔.

```
섹터 유입량 = marketPool × sectorFlowRate × marketMultiplier × trendMultiplier

내 점유율   = 내 섹터 자산 가치 / 섹터 총 자산 가치 (전체 Company)
내 소득     = floor(섹터 유입량 × 내 점유율 × dominanceMultiplier)
```

- 경쟁사가 같은 섹터에 있으면 파이를 나눠야 함
- 섹터 독점 = 파이 독식 (지배력 보너스와 시너지)
- 시장 풀이 줄어들면 전체 수익 감소 (제로섬에 가까워짐)

### 돈의 흐름

```
시장 풀 → 섹터 유입 → 점유율 분배 → 각 Company
각 Company → 매입 비용 → 시장 풀
각 Company → 생활비/지출 → 시장 풀
각 Company → 이벤트 비용 → 시장 풀
시장 풀 → 이벤트 보상 → 각 Company
각 Company → 매각 → 시장 풀 → 매각 대금 → Company
```

---

## 유기적 상호작용 (4가지)

### ① 섹터 수요 효과

경쟁사의 투자가 해당 섹터 자산 가격에 영향:

- 투자 집중 섹터 → 매입 비용 프리미엄 (+10~20%)
- 경쟁사 매각 시 → 가격 하락 (매수 기회)

### ② 지배력 경쟁

- 섹터당 지배자는 1명만 (순자산 기준 우선권)
- 경쟁사가 지배 중인 섹터 진입 시 프리미엄 추가
- 지배권 역전 가능

### ③ 이벤트 파급

- 모든 Company가 이벤트에 대응 (human: UI 선택, AI: 전략 기반)
- 경쟁사의 선택이 시장에 파급:
  - 보수형이 금융위기 때 전부 매각 → 시장 추가 하락
  - 공격형이 기술혁신 때 올인 → 기술 섹터 과열

### ④ 순위 효과

- 1위: 영향력 +2/턴 (시장 주목)
- 꼴찌: 이벤트에서 "역전 기회" 선택지 해금

---

## 정보 비대칭 시스템

### 공개 정보 (무료)

| 정보 | 표시 위치 |
|------|----------|
| 순위 + 순자산 | 현황판 |
| 보유 섹터 (개수만) | 현황판 |
| 전략 힌트 ("공격적 행보") | 현황판 |
| 글로벌 시장 상태 | GameHeader |
| 순위 변동 (▲▼─) | 현황판 |

### 비밀 정보 (AP 1 소모)

| 조사 대상 | 얻는 정보 | 전략적 가치 |
|----------|----------|------------|
| 섹터 트렌드 | 다음 턴 방향 | 매입/매각 타이밍 |
| 경쟁사 포트폴리오 | 특정 기업 보유 자산 전체 | 지배권 경쟁 판단 |
| 경쟁사 전략 | 전략 타입 정확히 공개 | 다음 행동 예측 |
| 섹터 점유율 | 정확한 수익 분배 비율 | 진입 가치 판단 |
| 시장 풀 현황 | 섹터별 유입량 + 잔여 풀 | 거시 전략 |

- 영향력 40+ 시 조사 AP 무료 (기존 규칙 확장)
- **행동 vs 정보** 트레이드오프가 AP 시스템의 핵심 긴장감

---

## 턴 처리 순서

```
턴 시작
├── AP 회복 (모든 Company 동일)
│
├── Planning Phase
│   ├── Human: UI에서 행동 선택
│   ├── AI: 전략 패턴이 행동 결정
│   └── 모든 Company의 Action[] 수집
│
├── 행동 해결 (순서: 시드 RNG 셔플)
│   └── 순서에 따라 매입 선착순 발생 가능
│
├── Event Phase
│   ├── 이벤트 발생 → 모든 Company 선택
│   └── 효과 동시 적용
│
├── Resolution Phase
│   ├── 시장 풀 → 섹터별 수익 분배 (점유율)
│   ├── 지배력 재계산 (전체 Company)
│   ├── 영향력 감소 (전체 Company)
│   ├── 순위 갱신
│   └── 화폐 보존 검증 (assert)
│
└── Result Phase
    ├── 상세 결과 (내 기업)
    ├── 요약 알림 (경쟁사 주요 행동)
    └── 순위 변동
```

### 행동 순서의 긴장감

행동이 시드 RNG로 셔플된 순서대로 처리됨:
- 내가 사려던 자산을 경쟁사가 먼저 매입 가능
- 섹터 수요에 따라 순서대로 가격 상승
- 매 턴 "이번엔 내가 먼저일까?" → 시드 기반 운 요소

---

## 현황판 UI

포트폴리오 좌측 배치.

```
┌──────────────────┐  ┌───────────────────┐
│  📊 기업 순위      │  │  내 포트폴리오      │
│                   │  │                   │
│  1. 🏦 한강투자    │  │  [기존 Portfolio]   │
│     $12,400  ▲2  │  │                   │
│  2. ⭐ 내 기업     │  │                   │
│     $10,200  ▼1  │  │                   │
│  3. 🚀 밀레니엄    │  │                   │
│     $8,900   ─   │  │                   │
│  4. 🎯 청담캐피탈  │  │                   │
│     $7,100   ▼1  │  │                   │
│                   │  │                   │
│  턴 15/30         │  │                   │
│  시장 풀: $14,200  │  │                   │
└──────────────────┘  └───────────────────┘
```

- 순위 변동 화살표 (▲▼─)
- 지배 섹터 아이콘 표시
- 클릭 → 경쟁사 공개 정보 모달
- 시장 풀 잔여량은 공개/비밀 선택 가능

---

## 아키텍처 변경

```
src/game/
├── types.ts          # Player 타입 → Company 타입으로 통합
├── competitor/
│   ├── types.ts      # CompetitorStrategy 인터페이스
│   ├── strategies.ts # 4개 전략 구현체
│   ├── ai.ts         # AI 의사결정 (Strategy 호출)
│   ├── names.ts      # 한국형 기업명 풀
│   └── index.ts
├── engine.ts         # Company[] 순회, Action 처리
├── economy.ts        # 시장 풀 분배, 점유율 계산
├── market.ts         # 섹터 수요/가격에 Company 영향
└── run.ts            # 초기 화폐 분배, Company[] 생성

src/components/game/
├── Leaderboard.tsx   # 현황판 (신규)
├── CompanyDetail.tsx  # 경쟁사 정보 모달 (신규)
└── ...

src/stores/
├── gameStore.ts      # companies[] 통합 상태
└── ...
```

`src/game/competitor/`도 순수 TypeScript — React import 금지.
